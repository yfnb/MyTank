package com;

import gameFactory.BaseBullet;
import gameFactory.BaseTank;

import java.awt.*;

public class BulletOne extends BaseBullet {


    public BulletOne(int x, int y, Dir dir, Group group) {
        super(x, y, dir, group);
    }
}
